# ChromaDB Playground

ChromaDB의 모든 JavaScript/TypeScript API를 테스트할 수 있는 인터랙티브 플레이그라운드입니다.

## 🚀 Quick Start

### 로컬 개발

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

http://localhost:3000 에서 ChromaDB 서버에 연결하세요.

### Docker 배포

```bash
# 이미지 빌드
docker build -t chromadb-playground .

# 컨테이너 실행
docker run -p 3000:3000 chromadb-playground
```

또는 Docker Compose 사용:

```bash
docker-compose up -d
```

## 🔌 연결 설정

플레이그라운드를 ChromaDB 서버에 연결하세요:

| 설정 | 기본값 | 설명 |
|------|--------|------|
| Host | `localhost` | ChromaDB 서버 주소 |
| Port | `8000` | ChromaDB 서버 포트 |
| Tenant | `default_tenant` | 테넌트 이름 |
| Database | `default_database` | 데이터베이스 이름 |

### ChromaDB 실행 예시

```bash
# Docker 이미지 빌드
docker build -t chromadb-playground .
# 실행 (ChromaDB URL을 환경변수로 지정)
docker run -p 3000:3000 -e CHROMA_URL=http://chromadb-server:8000 chromadb-playground
```

## 📚 지원 API

### Client API
- `heartbeat()` - 서버 연결 확인
- `version()` - ChromaDB 버전 조회
- `reset()` - 데이터베이스 초기화 (⚠️ 모든 데이터 삭제)
- `listCollections()` - 컬렉션 목록 조회
- `countCollections()` - 컬렉션 수 조회

### Collection 관리
- `createCollection()` - 새 컬렉션 생성
- `getCollection()` - 기존 컬렉션 조회
- `getOrCreateCollection()` - 컬렉션 조회 또는 생성
- `deleteCollection()` - 컬렉션 삭제

### Collection 작업
- `add()` - 문서/임베딩 추가
- `upsert()` - 문서 추가 또는 업데이트
- `get()` - 문서 조회
- `query()` - 유사도 검색
- `update()` - 문서 업데이트
- `delete()` - 문서 삭제
- `peek()` - 샘플 데이터 미리보기
- `count()` - 문서 수 조회
- `modify()` - 컬렉션 수정

## 📦 프로젝트 구조

```
playchroma/
├── src/
│   ├── main.ts              # 애플리케이션 진입점
│   ├── api/
│   │   └── methods.ts       # API 메서드 정의
│   ├── services/
│   │   └── chromaService.ts # ChromaDB REST 클라이언트
│   ├── store/
│   │   └── appStore.ts      # 상태 관리
│   ├── components/
│   │   └── Toast.ts         # 토스트 알림
│   └── styles/
│       └── main.css         # CSS 디자인 시스템
├── Dockerfile               # Docker 빌드 설정
├── docker-compose.yml       # Docker Compose 설정
└── nginx.conf               # Nginx 설정
```

## 🐳 Docker 아키텍처

```
┌──────────────────────────────────────┐
│  Docker Container (플레이그라운드)    │
│  ┌────────────────────────────────┐  │
│  │  Frontend (정적 파일)           │  │
│  │  - JavaScript/HTML/CSS         │  │
│  │  - fetch('/api/v2/...')        │  │
│  └────────────────────────────────┘  │
│              │                        │
│              ▼                        │
│  ┌────────────────────────────────┐  │
│  │  Nginx (프록시)                 │  │
│  │  /api/* → $CHROMA_URL/api/*    │  │
│  └────────────────────────────────┘  │
└──────────────────│───────────────────┘
                   │ HTTP 요청
                   ▼
┌──────────────────────────────────────┐
│  ChromaDB 서버 (별도 실행)            │
│  http://chromadb-server:8000         │
└──────────────────────────────────────┘
```

## 📝 License

MIT
